# 공지사항 v1.2.4

> **등록 완료** — id: `60b71daa-7f35-4e66-a503-00bbbc37398e` (2026-09-19 등록).

## title

v1.2.4 업데이트: AI 비서가 애매한 요청은 먼저 확인해요

## tag

업데이트

## body

```
새로워진 점을 알려드려요!

• AI 비서가 애매한 요청은 바로 실행하지 않고 "이거 맞을까요?" 먼저 확인한 뒤 진행해요.
• 구독 등록 시 이름이 정확한지 확인이 필요한 경우 카드에 경고 문구가 표시돼요.
• 종료했던 구독을 다시 시작하면 상태가 정상적으로 초기화되도록 수정했어요.
• 그 외 여러 안정성 개선이 포함되었습니다.

📱 안드로이드 앱 사용자는 최신 버전(v1.2.4)을 다운로드해 주세요!
다운로드: https://subly-web.product-notify23.workers.dev/releases/android
```

---

## 반영 방법

### 기존 공지를 수정하려면 (PATCH):

```bash
cd /Users/chan/Developer/seongnam-ai-camp/projects/subly
source <(cat .env web/.env 2>/dev/null | grep -E "SUPABASE")

curl -s -X PATCH \
  -H "apikey: ${SUPABASE_SERVICE_ROLE_KEY}" \
  -H "authorization: Bearer ${SUPABASE_SERVICE_ROLE_KEY}" \
  -H "content-type: application/json" \
  -H "prefer: return=representation" \
  "${EXPO_PUBLIC_SUPABASE_URL}/rest/v1/announcements?id=eq.60b71daa-7f35-4e66-a503-00bbbc37398e" \
  -d '{
    "title": "여기에 수정한 title",
    "body": "여기에 수정한 body",
    "tag": "업데이트"
  }'
```
