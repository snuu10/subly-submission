# 공지사항 v1.2.3

> 이 파일을 수정한 뒤, 아래 SQL(또는 스크립트)로 `public.announcements` 테이블에 반영하세요.
> **등록 완료** — id: `83b888c5-cdb5-449c-b95c-1048dd4d43e8` (2026-09-18 등록).
>
> **주의**: 이후 수정 시에도 업로드 전 사용자 컨펌 필수.

## title

v1.2.3 업데이트: 해지 안내 개선

## tag

업데이트

## body

```
새로워진 점을 알려드려요!

• 해지 방법 안내가 더 간단해졌어요 — 결제 경로를 따로 묻지 않고 바로 웹 결제 기준으로 안내해요. 앱스토어·플레이스토어·통신사로 결제하셨다면 해당 앱이나 고객센터에서 확인해 주세요.
• 해지 절차를 못 찾은 경우에도 "목록에서 삭제" 버튼으로 바로 정리할 수 있어요.
• 무료체험 종료 예정 알림 카드에 "계속 사용"/"결제 전 취소" 버튼을 추가했어요.
• 그 외 여러 안정성 개선이 포함되었습니다.

📱 안드로이드 앱 사용자는 최신 버전(v1.2.3)을 다운로드해 주세요!
다운로드: https://subly-web.product-notify23.workers.dev/releases/android
```

---

## 반영 방법

### 새 공지사항으로 등록하려면 (INSERT) — v1.2.3은 이 방법을 사용:

```bash
cd /Users/chan/Developer/seongnam-ai-camp/projects/subly
source <(cat .env web/.env 2>/dev/null | grep -E "SUPABASE")

curl -s -X POST \
  -H "apikey: ${SUPABASE_SERVICE_ROLE_KEY}" \
  -H "authorization: Bearer ${SUPABASE_SERVICE_ROLE_KEY}" \
  -H "content-type: application/json" \
  -H "prefer: return=representation" \
  "${EXPO_PUBLIC_SUPABASE_URL}/rest/v1/announcements" \
  -d '{
    "title": "v1.2.3 업데이트: 해지 안내 개선",
    "body": "새로워진 점을 알려드려요!\n\n• 해지 방법 안내가 더 간단해졌어요 — 결제 경로를 따로 묻지 않고 바로 웹 결제 기준으로 안내해요. 앱스토어·플레이스토어·통신사로 결제하셨다면 해당 앱이나 고객센터에서 확인해 주세요.\n• 해지 절차를 못 찾은 경우에도 \"목록에서 삭제\" 버튼으로 바로 정리할 수 있어요.\n• 무료체험 종료 예정 알림 카드에 \"계속 사용\"/\"결제 전 취소\" 버튼을 추가했어요.\n• 그 외 여러 안정성 개선이 포함되었습니다.\n\n📱 안드로이드 앱 사용자는 최신 버전(v1.2.3)을 다운로드해 주세요!\n다운로드: https://subly-web.product-notify23.workers.dev/releases/android",
    "tag": "업데이트"
  }'
```

### 기존 공지(예: 이 파일로 이미 등록한 v1.2.3 공지)를 수정하려면 (PATCH):

등록 후 응답으로 받은 새 `id`를 아래에 채워 사용하세요.

```bash
curl -s -X PATCH \
  -H "apikey: ${SUPABASE_SERVICE_ROLE_KEY}" \
  -H "authorization: Bearer ${SUPABASE_SERVICE_ROLE_KEY}" \
  -H "content-type: application/json" \
  -H "prefer: return=representation" \
  "${EXPO_PUBLIC_SUPABASE_URL}/rest/v1/announcements?id=eq.<발급받은 id>" \
  -d '{
    "title": "여기에 수정한 title",
    "body": "여기에 수정한 body",
    "tag": "업데이트"
  }'
```
